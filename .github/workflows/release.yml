name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: read

env:
  REGISTRY: ghcr.io
  # REPO is normalized to lowercase per job via the "Normalize repository name"
  # step, because GHCR image names must be lowercase and GitHub Actions
  # expressions have no lower() function. github.repository can contain
  # uppercase letters (e.g. seebom-labs/BOMHort).

jobs:
  images:
    name: Build & Push Images
    runs-on: ubuntu-latest
    permissions:
      packages: write
      id-token: write       # For cosign keyless signing & provenance attestation
      attestations: write   # For GitHub artifact attestations
    strategy:
      matrix:
        component:
          - name: ingestion-watcher
            context: backend
            target: ingestion-watcher
          - name: parsing-worker
            context: backend
            target: parsing-worker
          - name: api-gateway
            context: backend
            target: api-gateway
          - name: cve-refresher
            context: backend
            target: cve-refresher
          - name: ui
            context: ui
            target: ""
    steps:
      - name: Normalize repository name (GHCR requires lowercase)
        run: |
          REPO=$(printf '%s' "$GITHUB_REPOSITORY" | tr '[:upper:]' '[:lower:]')
          echo "REPO=$REPO" >> "$GITHUB_ENV"

      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: docker/setup-buildx-action@bb05f3f5519dd87d3ba754cc423b652a5edd6d2c # v4.2.0

      - uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract version from tag
        id: version
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Build and push
        id: push
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
        with:
          context: ${{ matrix.component.context }}
          target: ${{ matrix.component.target || '' }}
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.REPO }}/${{ matrix.component.name }}:${{ steps.version.outputs.version }}
            ${{ env.REGISTRY }}/${{ env.REPO }}/${{ matrix.component.name }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Install cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2

      - name: Sign container image
        env:
          DIGEST: ${{ steps.push.outputs.digest }}
        run: |
          cosign sign --yes \
            "${{ env.REGISTRY }}/${{ env.REPO }}/${{ matrix.component.name }}@${DIGEST}"

      - name: Attest provenance
        uses: actions/attest-build-provenance@4d101475d8b20a2381f78447822ac1eab6504dd8 # v4.2.2
        with:
          subject-name: ${{ env.REGISTRY }}/${{ env.REPO }}/${{ matrix.component.name }}
          subject-digest: ${{ steps.push.outputs.digest }}
          push-to-registry: true

  helm:
    name: Package & Push Helm Chart
    runs-on: ubuntu-latest
    needs: images
    permissions:
      packages: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: azure/setup-helm@9bc31f4ebc9c6b171d7bfbaa5d006ae7abdb4310 # v5.0.1

      - name: Normalize repository name (GHCR requires lowercase)
        run: |
          REPO=$(printf '%s' "$GITHUB_REPOSITORY" | tr '[:upper:]' '[:lower:]')
          echo "REPO=$REPO" >> "$GITHUB_ENV"

      - name: Extract version from tag
        id: version
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Update chart version
        run: |
          sed -i "s/^version:.*/version: ${{ steps.version.outputs.version }}/" deploy/helm/bomhort/Chart.yaml
          sed -i "s/^appVersion:.*/appVersion: \"${{ steps.version.outputs.version }}\"/" deploy/helm/bomhort/Chart.yaml

      - name: Package chart
        run: helm package deploy/helm/bomhort/

      - name: Login to GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | helm registry login ${{ env.REGISTRY }} -u ${{ github.actor }} --password-stdin

      - name: Push chart
        run: helm push bomhort-${{ steps.version.outputs.version }}.tgz oci://${{ env.REGISTRY }}/${{ env.REPO }}/charts

  github-release:
    name: Create GitHub Release
    runs-on: ubuntu-latest
    needs: [images, helm]
    permissions:
      contents: write
      id-token: write
      attestations: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Normalize repository name (GHCR requires lowercase)
        run: |
          REPO=$(printf '%s' "$GITHUB_REPOSITORY" | tr '[:upper:]' '[:lower:]')
          echo "REPO=$REPO" >> "$GITHUB_ENV"

      - name: Extract version from tag
        id: version
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Generate SBOM
        uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610 # v0.18.0
        with:
          path: .
          format: spdx-json
          output-file: bomhort-${{ steps.version.outputs.version }}.spdx.json
          artifact-name: bomhort-sbom

      - name: Install cosign
        uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2

      - name: Sign release artifacts
        run: |
          cosign sign-blob --yes \
            --bundle bomhort-${{ steps.version.outputs.version }}.spdx.json.bundle \
            bomhort-${{ steps.version.outputs.version }}.spdx.json

      - name: Attest SBOM provenance
        id: provenance
        uses: actions/attest-build-provenance@4d101475d8b20a2381f78447822ac1eab6504dd8 # v4.2.2
        with:
          subject-path: bomhort-${{ steps.version.outputs.version }}.spdx.json

      - name: Rename provenance bundle
        run: |
          cp "${{ steps.provenance.outputs.bundle-path }}" bomhort-${{ steps.version.outputs.version }}.spdx.json.intoto.jsonl

      - name: Create GitHub Release
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v2.3.2
        with:
          tag_name: ${{ github.ref_name }}
          name: BOMHort ${{ steps.version.outputs.version }}
          generate_release_notes: true
          files: |
            bomhort-${{ steps.version.outputs.version }}.spdx.json
            bomhort-${{ steps.version.outputs.version }}.spdx.json.bundle
            bomhort-${{ steps.version.outputs.version }}.spdx.json.intoto.jsonl
          body: |
            ## Container Images

            All images are signed with [cosign](https://github.com/sigstore/cosign) (keyless) and attested with SLSA provenance.

            ```
            docker pull ${{ env.REGISTRY }}/${{ env.REPO }}/api-gateway:${{ steps.version.outputs.version }}
            docker pull ${{ env.REGISTRY }}/${{ env.REPO }}/parsing-worker:${{ steps.version.outputs.version }}
            docker pull ${{ env.REGISTRY }}/${{ env.REPO }}/ingestion-watcher:${{ steps.version.outputs.version }}
            docker pull ${{ env.REGISTRY }}/${{ env.REPO }}/cve-refresher:${{ steps.version.outputs.version }}
            docker pull ${{ env.REGISTRY }}/${{ env.REPO }}/ui:${{ steps.version.outputs.version }}
            ```

            Verify signatures:
            ```bash
            cosign verify --certificate-identity-regexp="https://github.com/seebom-labs/bomhort" \
              --certificate-oidc-issuer="https://token.actions.githubusercontent.com" \
              ${{ env.REGISTRY }}/${{ env.REPO }}/api-gateway:${{ steps.version.outputs.version }}
            ```

            ## Helm Chart

            ```bash
            helm install bomhort oci://${{ env.REGISTRY }}/${{ env.REPO }}/charts/bomhort --version ${{ steps.version.outputs.version }}
            ```

            ## SBOM

            An SPDX SBOM for this release is attached as `bomhort-${{ steps.version.outputs.version }}.spdx.json`.
          draft: false
          prerelease: ${{ contains(github.ref_name, '-rc') || contains(github.ref_name, '-alpha') || contains(github.ref_name, '-beta') }}
